# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Vercel
.vercel/

# 로컬 개발용 데이터/로그 (배포·저장소 불필요 — 데이터는 /api/update 로만 받음)
*.csv
*.log

# OS / 에디터
.DS_Store
Thumbs.db
.env
.env.*
# 이름만 적힌 예시는 커밋한다(실제 값은 넣지 않는다).
# ※ .gitignore 는 줄 끝 주석을 지원하지 않는다 — 주석은 반드시 별도 줄에.
!.env.example

tmp/
